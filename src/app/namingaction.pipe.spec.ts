import { NamingactionPipe } from './namingaction.pipe';

describe('NamingactionPipe', () => {
  it('create an instance', () => {
    const pipe = new NamingactionPipe();
    expect(pipe).toBeTruthy();
  });
});
